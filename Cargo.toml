[package]
name = "duck-diagnostic"
version = "0.1.0"
edition = "2021"
authors = ["wildduck"]
description = "A generic, compiler-agnostic diagnostic engine for building rich error reporting into any tool — compilers, interpreters, linters, query engines, and more."
license = "MIT"
repository = "https://github.com/gentleduck/duck-diagnostic"
keywords = ["diagnostic", "compiler", "error-reporting", "linter"]
categories = ["development-tools", "compilers"]

[dependencies]
colored = "3"
